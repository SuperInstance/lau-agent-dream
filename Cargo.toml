[package]
name = "lau-agent-dream"
version = "0.1.0"
edition = "2021"
description = "The dream cycle — what happens when an agent is idle"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"
